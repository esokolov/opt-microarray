function arrays = pick_arrays(I, arraysNum)
s = sum(I);
boundaries = quantile(s, [0 0.25 0.5 0.75 1]);
arrays = [];       
for i=1:4
    Carrays = find(s<=boundaries(i) & s>=boundaries(i+1));
    pick = randperm(length(Carrays));
    arrays = [arrays Carrays(pick(1:(i*floor(length(pick)/4)))];
end